# Dependências do Node (não devem ir para o GitHub)
node_modules/

# Ficheiros de variáveis de ambiente (contêm URLs e dados sensíveis)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Ficheiros de compilação / Build
dist/
build/

# Ficheiros do sistema operativo / IDE
.DS_Store
.vscode/
.idea/
*.log